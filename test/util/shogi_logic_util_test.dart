import 'package:flutter_test/flutter_test.dart';
import 'package:quiver/iterables.dart';

void main() {
  test('test', () {
    print(range(8 - 1, -1, -1));

    for (int r = 0 + 1; r > 8; r++) {
      print(r);
    }
  });
}
